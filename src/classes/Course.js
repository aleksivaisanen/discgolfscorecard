//Course data model
export default class Course {

    constructor(courseName, parArray, id) {
        this.courseName = courseName,
            this.parArray = parArray,
            this.id = id
    }
}