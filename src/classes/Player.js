//player data model
export default class Player {
    constructor(playerName, playerNickname, profilepic, id) {
        this.playerName = playerName,
            this.playerNickname = playerNickname,
            this.profilepic = profilepic
        this.id = id
    }
}